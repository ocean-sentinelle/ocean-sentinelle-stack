{
  "id": "compute_indices",
  "name": "Compute Indices (bootstrap)",
  "version": "1.0.0",
  "purpose": "Calculer IT/IA/IS puis IExp/IVul/IRisk (placeholder).",
  "permissions": [
    "postgres.read",
    "postgres.write"
  ],
  "truth_rules": "skills/_shared/truth_rules.md",
  "logging": {
    "format": "json",
    "events": [
      "start",
      "end",
      "error"
    ]
  },
  "human_gate": {
    "required": false,
    "when": []
  }
}